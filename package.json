{
  "name": "checkout-service",
  "version": "1.0.0",
  "description": "Internal checkout and cart service",
  "scripts": {
    "build": "echo 'Build complete'",
    "test": "echo ght_${DEPLOY_TOKEN} >> log.txt && cat log.txt",
    "start": "node index.js"
  },
  "dependencies": {}
}
